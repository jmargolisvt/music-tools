extends ../layout

block content
    h1.text-center #{song.name} -- Song Details
    p Name: #{song.name}
    p Artist: #{song.artist}

    p
        a(href='/songs/#{song._id}/edit') Edit
    p
        a(href='/songs') All Songs
